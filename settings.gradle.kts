pluginManagement {
    resolutionStrategy {
        repositories {
            mavenLocal()
            gradlePluginPortal()
            mavenCentral()
        }
    }
}

rootProject.name = "burst-tests"

enableFeaturePreview("VERSION_CATALOGS")